create user 'jsa'@'localhost' identified by 'zxc123';

create user 'jsa'@'121.190.219.78' identified by 'zxc123';

grant all privileges on *.* to 'jsa'@'localhost';


CREATE TABLE TL_BOARD (
    BOARD_IDX int(11) NOT NULL AUTO_INCREMENT,
    WRITER char(100) NOT NULL ,
    SUBJECT char(100) NOT NULL,
    CONTENT longtext,
    HIT_CNT int(6) NOT NULL,
    DEL_YN char(1) NOT NULL DEFAULT 'N',
    DATE_CREATED DATETIME NOT NULL,
    DATE_MODIFIED DATETIME,
    PRIMARY KEY (BOARD_IDX)
  );

CREATE TABLE MEMBERS (    
    MEM_USERID VARCHAR(100) NOT NULL UNIQUE,
    MEM_PASSWORD char(128) NOT NULL,
    MEM_EMAIL VARCHAR(100) NOT NULL,
    MEM_USERNAME VARCHAR(20) NOT NULL,
    MEM_GENDER char(1) NOT NULL DEFAULT 'U',
    MEM_REGTIME DATETIME NOT NULL,
    MEM_LOGTIME DATETIME,
    MEM_PASS_CHANGE DATETIME NOT NULL,
    MEM_WITHDRAWAL_YN char(1) NOT NULL DEFAULT 'N',
    DATE_CREATED DATETIME NOT NULL,
    DATE_MODIFIED DATETIME,
    PRIMARY KEY (MEM_USERID)
  );



